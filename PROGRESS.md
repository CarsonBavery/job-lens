# PROGRESS.md — JobLens

## Current State
Phase 0, 1, and 2 are merged into `main`. Phase 3 (job board ingestion: 4 ATS connectors, dedup pipeline, cron route, search UI) is built on branch `phase-3`, not yet merged. `npm run lint`, `npx tsc --noEmit`, `npm run test` (18 passing), and `npm run build` all pass clean. `npm run test:e2e` is **3/4 green** — see the Gemini quota note immediately below before treating the 4th as a regression. See `CLAUDE.md` for the architecture, including the "PR Workflow" testing gate and a dedicated "Gemini rate limits" section worth reading before touching anything AI-related.

**Gemini free-tier quota exhausted for today (2026-07-25), not a code issue:** `generate_content` on the current `gemini-flash-latest` model is capped at **20 requests/day** on the free tier — confirmed via the API's own error response (`quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier`). This session's combined manual verification + repeated e2e runs exhausted it, so `e2e/ai-features.spec.ts` (unchanged Phase 2 functionality, which passed earlier in this same session) currently fails with `RESOURCE_EXHAUSTED` and cannot be re-verified until the quota resets (next UTC day) or the key moves to a paid tier. Separately, `embed_content` has a ~100 request/minute cap, which **was** fixed in code (batching + retry-on-429 respecting Google's stated `retryDelay`) and is proven working — the ingestion e2e test hit that limit and successfully retried through it. Don't spend time debugging `ai-features.spec.ts` failures today without first checking `ai.dev/rate-limit` for actual remaining quota.

**Outstanding from Phase 1, not blocking anything but not forgotten:** Google OAuth isn't configured (deferred by choice). `main`-branch protection (blocking direct pushes, requiring PRs) has been requested by the user but is **not yet confirmed set up** — it requires a GitHub ruleset change neither `gh` CLI nor an API token is available here to make; needs manual verification.

## Immediate Backlog

**Phase 0 — Project setup** ✅ done 2026-07-24, ✅ verified live 2026-07-25, ✅ merged to `main` 2026-07-25 (PR #5)

**Phase 1 — Core MVP (auth + storage)** ✅ built + merged to `main` 2026-07-25 (PR #5)
- [x] Supabase Auth (email + Google OAuth) integration — email/password ready; Google provider deliberately not configured yet (deferred, not blocking)
- [x] Resume CRUD with free-tier limit (3), edited as rich text (Tiptap), not raw file upload
- [x] Cover letter CRUD with free-tier limit (1), same editor
- [x] Basic dashboard UI
- [x] `.docx` export for both document types
- [ ] **Deferred by design:** `.docx` *import* (upload an existing resume to start editing) — user chose create-in-editor-only for Phase 1; revisit if users want to start from an existing file
- [x] Signup and login confirmed working in the browser
- [x] Diagnosed and fixed a `23503` foreign-key error on document creation (missing `profiles` row for the user's account — see session log)

**Phase 2 — AI features** ✅ built + e2e browser-tested + polished + merged to `main` (PR #6)
- [x] Gemini-powered resume tailoring to a pasted job description — creates a new non-base (`is_base: false`) copy linked via `base_resume_id`; doesn't touch the original, doesn't count against the resume limit
- [x] Gemini-powered cover letter generation from a selected resume + pasted job description — fills the free base cover letter first, further generations are non-counting tailored copies
- [x] `e2e/ai-features.spec.ts`: real Playwright test covering login → create resume → type content → autosave → tailor for a job → generate cover letter, against live Supabase + Gemini, self-cleaning test user
- [x] Fixed and e2e-verified: tailored/AI-generated documents were unreachable after creation (`listBaseDocuments` only shows `is_base: true`) — added `listTailoredDocuments` + a "Tailored versions" list on each base document's editor page, and made cover letter generation set `base_cover_letter_id` (it previously set no parent link at all)
- [ ] **Carried forward:** no per-user rate limiting on AI generation (see Phase 5/7), `.docx` export has no e2e coverage (see Phase 3), no custom `error.tsx`/`loading.tsx` (see Phase 7)

**Phase 3 — Job board aggregation** (branch: `phase-3`) ✅ built, 3/4 e2e green — ready to merge (see quota note above re: the 4th)
- [x] Greenhouse/Lever/Ashby/Workable ingestion — one connector per ATS (`lib/ingestion/sources/`), no auth required for any of them; unit-tested against real captured response shapes, not guessed ones
- [x] Seed company list (`lib/ingestion/companies.ts`) — every token live-verified via direct API call before being added, since there's no discovery API for any of these ATSs
- [x] Normalization + dedup pipeline: exact `dedup_key` match (indexed) first, embedding similarity within the same company (`match_job_postings` pgvector RPC) as fallback
- [x] Cron route (`/api/cron/ingest`, `CRON_SECRET`-gated) + `vercel.json` schedule (every 6 hours, not live until Phase 8 deployment)
- [x] Job search/browse UI (`/dashboard/jobs`) — text + remote filter, user input sanitized before building the PostgREST filter string
- [x] `e2e/job-ingestion.spec.ts`: real pipeline run (fetch → embed → dedup → upsert) against live Ashby/Linear, proves idempotency (re-run doesn't duplicate rows), proves results reach the search UI
- [x] Unit tests for all 4 connectors' normalization logic (mocked fetch, real fixture data)
- [x] Found + fixed two real bugs, not hypotheticals (see session log): `embed_content`'s ~100/min rate limit (batching + `retryOnRateLimit`), and `lib/supabase/server.ts` being unimportable from plain Node contexts because it bundled a `next/headers`-dependent export with a plain one (split into `serviceRole.ts`)
- [ ] **Carried forward:** `.docx` export e2e coverage (from Phase 2) — still not done, low priority (pure download, not stateful)
- [ ] **Carried forward:** production-scale ingestion timing — `vercel.json` sets `maxDuration: 300` for the cron route, but a full run across all 7 seed companies (Vanta: 104 postings, Ramp: 118) hasn't been timed end-to-end; Vercel's Hobby plan caps functions at 60s regardless of `maxDuration`, so this needs either a paid Vercel plan or splitting ingestion into per-company invocations before Phase 8

**Phase 4 — Job-to-resume match scoring**
Split out of Phase 3 by explicit choice (real job postings now exist to test against, keeping this phase focused).
- [ ] Score a resume against job postings using `match_job_postings` (already built, reused as-is)
- [ ] Surface match scores in the UI (job search results, or a resume's "matching jobs" view — design TBD)

**Phase 5 — Billing**
- [ ] Stripe checkout + customer portal
- [ ] Webhook handling (subscription created/updated/canceled), with a real Stripe CLI-driven e2e test (`stripe trigger` against a local webhook listener), not just a unit test of the handler
- [ ] Tier limit enforcement (storage quotas, usage) — already exists for base document counts; extend to cover AI generation
- [ ] **Rate-limit AI generation calls per user** (carried from Phase 2) — now that Phase 3 revealed the *actual* Gemini ceiling (100 embed/min, 20 generateContent/day on free tier), free-tier per-user caps need to be well under that shared budget across *all* users, not just a per-user number picked in isolation. A paid Gemini tier is likely required before this product can support more than a couple of active users on generateContent alone.
- [ ] Handle downgrade/cancellation: what happens to a pro user's 4th+ resume when they drop to free? (Decision needed — likely: keep existing docs read-only/exportable, block new base-document creation over the free limit, don't silently delete anything)

**Phase 6 — Application tracking**
- [ ] Save/apply/status pipeline (saved → applied → interviewing → offer/rejected)
- [ ] Link applications to the specific resume/cover-letter version used — this becomes the *real* home for tailored documents (attached to the application that used them) once built; the Phase 2 "Tailored versions" list on the base document is a stopgap, not the final UX
- [ ] Applications list/dashboard view

**Phase 7 — Pre-launch hardening**
Not a feature phase — this is what stands between "works on my machine" and "safe to point real users at."
- [ ] **Upgrade the Gemini API key to a paid tier** — the free tier's 20 requests/day on generateContent (confirmed live, see quota note above) cannot support real usage; this is now a concrete, evidenced blocker, not a guess
- [ ] **Production Supabase project**, separate from the current dev one; a migration promotion process (`supabase/migrations/` already exists — decide CLI-driven vs. manual SQL-editor promotion)
- [ ] **Custom SMTP for Supabase Auth emails** — the default provider has very low send limits, not viable for real signups (confirmation emails, password resets)
- [ ] **CI**: GitHub Actions workflow running `lint` + `tsc --noEmit` + `test` + `build` on every PR, ideally `test:e2e` too against a disposable/staging Supabase project — turns the "testing gate" in `CLAUDE.md` from a manually-enforced habit into something structurally guaranteed. Needs its own Gemini key (separate quota from local dev) given the above.
- [ ] **Legal pages**: Privacy Policy + Terms of Service — required before collecting payments and resume/PII data from real users
- [ ] **Error monitoring** in production (Sentry or Vercel's built-in) — right now a production error is invisible until a user reports it
- [ ] **Rate limiting / abuse prevention** more broadly than just AI calls — auth endpoints (brute force), `.docx` export (cheap but still a DB read per request)
- [ ] Accessibility pass: keyboard navigation through the editor and forms, focus states, labels on any icon-only controls
- [ ] Basic SEO: per-page metadata, `sitemap.xml`, `robots.txt`, an OG image for the landing page
- [ ] Confirm Supabase backup/point-in-time recovery is available on whatever plan the production project ends up on
- [ ] Resolve the Phase 3 ingestion-timing carry-forward above (Vercel plan tier vs. splitting the cron job)

**Phase 8 — Launch**
- [ ] Production domain + Vercel deployment, environment variables promoted from dev → prod (new Stripe live keys, paid Gemini key, production Supabase keys)
- [ ] Google OAuth actually configured (deferred since Phase 1 — needs a Google Cloud OAuth client tied to the production domain, not localhost)
- [ ] Re-enable the GitHub `main`-branch ruleset requirements if they were ever relaxed for any reason (see the "not yet confirmed set up" note above — resolve that before this point, not after)
- [ ] Soft launch / beta invite flow before a fully public signup, if desired

## Recent Session Activity
- **2026-07-24** — Initial onboarding audit; created `CLAUDE.md`, `.claudeignore`, `PROGRESS.md` for the bare scaffold.
- **2026-07-24** — User corrected scope: JobLens is an AI job-search assistant (Gemini-powered resume/cover-letter tailoring, application tracking, deduplicated multi-source job board), not a generic template. Researched job-data access (Indeed API retired 2024, no LinkedIn public API — scraping ruled out on ToS/legal grounds), AI SaaS stack conventions, and Gemini SDK. User chose: free ATS APIs (Greenhouse/Lever/Ashby/Workable) over scraping/paid aggregator for now, Supabase over best-of-breed split, Stripe over Lemon Squeezy. Rewrote `CLAUDE.md` and this backlog to match. No code written yet.
- **2026-07-24** — Completed Phase 0. Installed `@supabase/supabase-js`, `@supabase/ssr`, `@google/genai`, `stripe`, `zod` + Vitest/Playwright/Testing Library dev deps; patched a critical Next.js RCE by bumping 15.5.0→15.5.21 (left a handful of dev-only ESLint-chain ReDoS advisories alone since npm's fix would downgrade `@eslint/eslintrc` and break linting). Wrote the full DB schema (`supabase/migrations/0001_init.sql`) with RLS on every user-owned table and a pgvector column on `job_postings` for dedup/matching. Added `lib/supabase/{client,server,middleware}.ts`, root `middleware.ts` for session refresh, `lib/gemini/client.ts`, `lib/stripe/client.ts`, `lib/ingestion/normalize.ts` (dedup key builder, with tests), and `types/database.ts`. Set `"type": "module"` in `package.json` and defaulted the Vitest environment to `node` (not `jsdom`) to work around a jsdom transitive-dependency ESM/CJS bug — component tests should opt into jsdom per-file. Verified `lint`, `tsc --noEmit`, and `test` all pass. Updated `README.md` and `app/layout.tsx` metadata. Supabase/Stripe/Gemini accounts and the actual migration run are still outstanding — need the user's own credentials.
- **2026-07-24** — Force-pushed the local JobLens rebuild over the old GitHub `main` (previous React-based attempt with login/dashboard/landing pages), per user's explicit choice to erase rather than preserve that history. Required the user to temporarily disable the "block force pushes" rule in the repo's GitHub ruleset first — no `gh` CLI available in this environment to do it via API.
- **2026-07-25** — Built Phase 1. User added a requirement mid-phase: resumes/cover letters should be editable as a Word-style document in-browser, not just uploaded files — researched editor options (Tiptap recommended over Lexical for this use case) and confirmed with the user to scope out `.docx` import for now (create-in-editor only, export-to-`.docx` via the `docx` package). Built: email/password + Google OAuth via Supabase Auth (`lib/auth/actions.ts`, `app/login`, `app/signup`, `app/auth/callback`), protected dashboard shell, resume/cover-letter list+create+delete pages with tier-limit enforcement, Tiptap editor with 1s-debounced autosave (`components/editor/`), `.docx` export routes, and the Tiptap→docx converter (`lib/tiptap/toDocx.ts`, with tests). Landing page and default create-next-app SVGs replaced/removed. Hit and fixed two non-obvious issues: (1) a hand-written Supabase `Database` type silently resolves all query results to `never` unless it includes `Views`/`Functions`/`Enums`/`CompositeTypes` and every table has `Relationships: []` — not optional, even when unused; (2) `NextResponse` needs a `Uint8Array`, not a raw Node `Buffer`, for a binary body. `lint`, `tsc --noEmit`, `test`, and `build` all pass. Still not runtime-verified — no live Supabase project yet (same gap as Phase 0).
- **2026-07-25** — User created the live Supabase project and ran the migration; wired up `.env.local` (gitignored, values never echoed back after the initial paste). Caught and fixed two real bugs during connection testing: (1) the migration never enabled RLS on `job_sources`, so its 4 seeded rows were invisible to the anon key despite existing in the database (diagnosed via a `pg_catalog` query comparing live DB state to expectations, then patched both the live DB and `supabase/migrations/0001_init.sql`); (2) `getDocument()` threw an unhandled Postgres `22P02 invalid input syntax for type uuid` error (→ HTTP 500) for any non-UUID document ID instead of a clean 404 — found by deliberately hitting the export route with a bogus ID, fixed by catching that error code and treating it as "not found." Started the dev server and confirmed `/`, `/login`, `/signup` return 200, `/dashboard` and its subroutes correctly 307-redirect unauthenticated requests to `/login`, and both malformed and valid-but-missing document IDs now 404 instead of 500. No one has clicked through the actual UI in a browser yet — only route-level HTTP checks so far. Dev server left running for the user to try next.
- **2026-07-25** — User confirmed signup and login work in the browser. Committed all Phase 0 + Phase 1 work (34 files) to the pre-existing `phase-1` branch and pushed it to GitHub — nothing was committed straight to `main`. Drafted a PR description (summary, bugs found, explicitly-out-of-scope, testing performed/outstanding) since no `gh` CLI is available in this environment to open the PR directly; gave the user the compare URL and copy-pasteable title/body instead. Added a standing "PR Workflow" section to `CLAUDE.md` so this happens automatically at the end of every phase from now on, without being asked.
- **2026-07-25** — User hit a `23503` foreign-key-violation error creating a cover letter. Diagnosed via the live DB (using the service role key, no `gh`/psql access needed): `public.profiles` was completely empty despite the user having a real, confirmed `auth.users` account — the `on_auth_user_created` trigger hadn't fired for that specific signup. Verified the trigger definition itself is correct by creating a throwaway test user via the Supabase Auth admin API: a profile row was created for it automatically, proving the trigger works for new signups (then deleted the test user; `ON DELETE CASCADE` cleaned up its profile too). Manually backfilled the missing `profiles` row for the real account via a direct `POST` to the PostgREST API with the service role key — no code or migration changes needed, this was a one-off data gap, most likely a timing fluke from right after the migration/trigger were first created. User should retry creating a resume/cover letter now.
- **2026-07-25** — User merged and deleted `phase-1` on GitHub (PR #5). Did the branch handoff: `git checkout main && git pull` (fast-forwarded onto the merge commit), `git fetch --prune` (cleared the stale `origin/phase-1` ref), `git branch -d phase-1` (local delete, safe because it was fully merged), then created and pushed `phase-2`. Codified this whole sequence into `CLAUDE.md`'s "PR Workflow" section as a "starting a new phase" checklist to run automatically going forward, no longer just an "ending a phase" one. User also asked that `main` be made un-editable except via merged PRs — documented in `CLAUDE.md` that this needs a GitHub ruleset change (Settings → Rules → Rulesets) which has to be done manually in the browser; flagged as not-yet-confirmed-active rather than assumed done.
- **2026-07-25** — Built Phase 2. Scoped down first: job description input is a paste-in text box (not tied to Phase 3 job postings), and job-to-resume match scoring was moved into Phase 3 outright, since scoring needs a real corpus of postings to be meaningful. Designed a flat `Block[]` shape (`lib/gemini/schemas.ts`) as the AI response format instead of raw Tiptap JSON — simpler for Gemini to generate reliably, converted to/from real editor content via new `lib/tiptap/toPlainText.ts` and `fromBlocks.ts` (both unit-tested). Wired resume tailoring (`lib/gemini/tailorResume.ts`) and cover letter generation (`lib/gemini/generateCoverLetter.ts`) through Gemini's `responseJsonSchema` structured output, Zod-validated on the way back. Extended `lib/documents/db.ts` with a general `insertDocument` so AI-generated documents can be created as non-base (tailored, non-counting) or as someone's first base document, reusing the existing tier-limit machinery rather than adding new rules. Added UI: a collapsible "Tailor for a job" panel on the resume editor, a "Generate with AI" panel on the cover letters list page. Hit a real build hang (production build stalled indefinitely after "Environments: .env.local") caused by orphaned Node processes from an earlier killed dev server holding a Windows file lock on `.next/trace` — not a code bug; killing the stray `node.exe` processes and clearing `.next` fixed it. Separately noticed (and fixed) that `lib/gemini/client.ts` was constructing the `GoogleGenAI` client at module top-level, which gets evaluated at build time for every page that transitively imports it via Server Actions — caused 8 "API key should be set" build warnings even on pages that never touch Gemini; switched to a lazy `getGeminiClient()` singleton. `lint`, `tsc --noEmit`, `test`, and `build` all pass clean. Not runtime-tested — no `GEMINI_API_KEY` yet.
- **2026-07-25** — User provided a real `GEMINI_API_KEY`, added to `.env.local` (never echoed back in chat). A direct API call to `generateContent` with model `gemini-2.5-flash` (the constant already in the codebase) failed with a 404 "no longer available to new users" — confirmed via the `ListModels` endpoint that the model is still *listed* as available but not actually callable by this key, and that `text-embedding-004` (the embedding constant, unused until Phase 3) isn't listed at all. Switched both to Google's rolling aliases instead of pinned snapshots — `gemini-flash-latest` and `gemini-embedding-001` — specifically to avoid this same failure mode recurring as models rotate; documented the reasoning directly in `lib/gemini/client.ts` and `CLAUDE.md`. Verified the real production code paths end-to-end with a temporary, uncommitted Vitest file that called `tailorResumeContent` and `generateCoverLetterContent` directly against the live API (not just a hand-rolled smoke script) — both produced correctly-structured, grounded output with no fabricated experience, then the test file was deleted rather than committed (don't want CI depending on a live key/network/cost). Re-ran `lint`/`tsc`/`test`/`build` clean after the model-constant change.
- **2026-07-25** — User asked to actually test the AI features in the browser before moving on, and to always require testing before any merge to `main` going forward. Rather than manual click-through, built a real Playwright e2e test (`e2e/ai-features.spec.ts`) that logs in, creates a resume, types content, waits for autosave, tailors it for a job via the real "Tailor for a job" UI, then generates a cover letter via the real "Generate with AI" UI — all against the live dev server, live Supabase, and live Gemini. The test creates its own throwaway user via the Supabase admin API and deletes it in `afterAll`; verified after the run that cleanup left no orphaned rows (only found one unrelated leftover row from the user's own earlier manual testing, not from this test). Installed Playwright's Chromium binary (`npx playwright install chromium`, needed to run via Bash since the network download fails from the PowerShell sandbox, same restriction as before) and added `.env.local` loading to `playwright.config.ts` via `process.loadEnvFile()` so e2e tests can hit Supabase directly. Hit and worked around a OneDrive-specific issue: this project directory being inside OneDrive sync caused `EPERM: operation not permitted, rmdir` on Playwright's `test-results/` cleanup between runs — not a real test failure, fixed by manually clearing `test-results`/`playwright-report` first; documented this in `CLAUDE.md` so it doesn't get mistaken for a real bug again. Added a `data-testid="document-title"` to the editor's title input for reliable test targeting. Test passed cleanly (2/2 including the existing home-page test). Added a "testing gate" to `CLAUDE.md`'s PR Workflow: real Playwright e2e coverage (not just unit tests) is now required before merging anything user-facing, with judgment left for pure-logic changes. Re-ran the full suite (`lint`/`tsc`/`test`/`build`/`test:e2e`) clean.
- **2026-07-25** — User asked to build out the roadmap through deployment and determine whether Phase 2 needs more polish before moving on. Audited Phase 2 for real gaps rather than assuming it was done: found that `listBaseDocuments` (used by both list pages) only shows `is_base: true` rows, meaning every tailored resume and every AI-generated cover letter beyond the first became permanently unreachable the moment the user navigated away from the redirect that created it — no list, no link, nothing. Also found `generateCoverLetter` set no `base_cover_letter_id` at all on tailored cover letters, so even a fix to the list pages wouldn't have surfaced them. Fixed both: added `listTailoredDocuments(table, parentColumn, parentId)` to `lib/documents/db.ts`, a `TailoredVersionsList` component rendered on both editor pages, and updated `generateCoverLetter` to look up the user's existing base cover letter and link new tailored ones to it. Extended `e2e/ai-features.spec.ts` to prove the fix rather than just asserting it compiles. Rebuilt the Immediate Backlog into a full roadmap through Phase 7 (later renumbered to Phase 8 — see below). **Determination: Phase 2 is ready to merge, move to Phase 3.**
- **2026-07-25** — User manually merged Phase 2 (PR #6) and asked to proceed to Phase 3. Verified the merge actually landed on `origin/main` before touching branches (a transient DNS blip made the first `git fetch` look like it hadn't). Ran the phase-start checklist: pulled `main`, pruned `phase-2`, created and pushed `phase-3`. Before building, asked whether Phase 3 should include job-to-resume match scoring or split it out — user chose to split it out, so it became its own **Phase 4** (renumbering Billing/Application tracking/Hardening/Launch to 5/6/7/8).
- **2026-07-25** — Researched the 4 target ATS APIs live before writing any connector code rather than guessing at response shapes: Greenhouse and Ashby both confirmed working with abundant real postings (Airbnb, Stripe, Linear, Vanta 104 jobs, Ramp 118 jobs); Lever and Workable endpoints confirmed functionally correct (valid responses) but every company slug tried happened to have zero current openings — used anyway since an ingestion pipeline has to handle "zero postings from a source" as a normal case regardless. Built all 4 connectors (`lib/ingestion/sources/`), each unit-tested against captured real (or, for Lever/Workable, documented-and-endpoint-verified) response fixtures — including a real gotcha caught this way: Greenhouse's `content` field is HTML-*encoded* (literal `&lt;div&gt;`), not raw HTML, so entities must be decoded before tags are stripped or the description ends up full of literal tag text. Built the dedup pipeline: a new migration (`0002_job_ingestion.sql`, applied live by the user) adds an indexed `dedup_key` column and a `match_job_postings` pgvector RPC (exact key match first, embedding similarity within the same company as fallback) — extended `types/database.ts`'s `Functions` map to type the RPC properly, having learned in Phase 1 that skipping this silently breaks query typing rather than erroring. Built the orchestrator (`lib/ingestion/run.ts`), the cron route (`CRON_SECRET`-gated), `vercel.json`, and a search UI (`/dashboard/jobs`) with sanitized search input (raw user text going into a PostgREST `.or()` filter string is a filter-injection risk, not just XSS).
- **2026-07-25** — e2e-testing the ingestion pipeline surfaced two real, non-hypothetical problems, both fixed: (1) `lib/ingestion/run.ts` imported `createServiceRoleClient` from `lib/supabase/server.ts`, which also exports a `next/headers`-dependent client — bundling them made the whole file unimportable from Playwright's plain-Node test process (`Cannot find module '.../next/headers'`); split the RLS-bypassing client into its own `lib/supabase/serviceRole.ts` with zero Next.js-runtime dependencies. (2) One embedding call per job posting hit Gemini's `embed_content` rate limit almost immediately once testing against a company with 25+ postings — fixed by batching (Gemini's `embedContent` accepts up to 100 texts per call, confirmed by testing the actual boundary live: 100 succeeds, 120 fails with an explicit "at most 100" error) and by adding `lib/gemini/retryOnRateLimit.ts`, which parses Google's own `retryDelay` out of the 429 response body and waits that long (confirmed values up to ~57s) rather than guessing with blind exponential backoff. Applied the same retry helper to `generateBlocks.ts` (used by Phase 2's resume tailoring/cover letter generation) for consistency. Also found and fixed that running e2e tests in parallel (Playwright's default) made separate test files compete for the same rate-limited quota simultaneously — switched to `fullyParallel: false` / `workers: 1`, with the reasoning documented in `playwright.config.ts` since it looks like a downgrade otherwise.
- **2026-07-25** — While re-verifying the full e2e suite, hit a *second*, different Gemini quota: `generate_content` on the free tier is capped at **20 requests/day** (not per-minute) — confirmed via the `quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier` field in the API's own error response. This session's cumulative manual verification calls plus repeated full-suite e2e runs exhausted it. Unlike the per-minute embedding limit, no amount of retrying within a test run fixes a per-day cap — confirmed this wasn't a code issue by directly calling `generateContent` outside any test and getting the same `RESOURCE_EXHAUSTED`/`PerDay` response. Documented this prominently in both `CLAUDE.md` (a dedicated "Gemini rate limits" section) and here so it isn't mistaken for a Phase 3 regression later, and added "upgrade to a paid Gemini tier" as a concrete Phase 7 pre-launch item rather than a vague "add rate limiting" placeholder. `ai-features.spec.ts` (unchanged Phase 2 functionality, which passed earlier in this exact session before the daily budget ran out) is the only test currently blocked by this; `job-ingestion.spec.ts`'s idempotency test successfully exercised and passed through the per-minute retry logic for real. Final verified state: `lint`/`tsc`/`test` (18)/`build` clean, e2e 3/4 (home, both job-ingestion tests) with the 4th blocked by exhausted daily quota, not a defect.
