# PROGRESS.md — JobLens

## Current State
Phase 0 through 7 are merged into `main`, plus the job-posting-staleness fix (see "Maintenance" below, merged via PR #9). **Phase 8 is in progress on branch `phase-8`, not yet ready for review** — its scope was redefined 2026-07-31 partway through (see the Phase 8 section below for the full rationale and checklist): what started as pure front-end visual polish became a product pivot to a STEM-job-board-primary app, with real per-posting discipline categorization, a job-listing-to-AI-tailoring bridge, a restructured dashboard IA, and a second full visual-identity rebuild (the warm-coral direction built earlier in the same phase was explicitly rejected before merge, independent of the IA change). `npm run lint`, `npx tsc --noEmit`, `npm run test` (53 passing), and `npm run build` are last verified clean on the pivot's latest commit; `npm run test:e2e` has new/updated specs for the pivot not yet run end-to-end (see Phase 8 checklist). Stripe is on a real test-mode account as of 2026-07-27 (no live/production keys yet, no real money involved) — `STRIPE_CLI_PATH` env var can override the CLI binary location for `e2e/billing.spec.ts` if it's not on PATH. The Gemini API key is on a paid tier as of 2026-07-26 — cost is a non-issue (~$0.01/action, ~$1–2 even for a heavy testing day), the free tier's actual problem had been the hard 20-requests/day ceiling regardless of size. See `CLAUDE.md` for the architecture, including the "PR Workflow" testing gate and the "Gemini rate limits" section (still relevant — the per-*minute* `embed_content` limit and the retry logic around it aren't paid-tier-specific).


## Maintenance — job posting staleness (fixed 2026-07-26/27, merged to `main` via PR #9)
Not a numbered phase — a real gap the user caught after Phase 4: the ingestion pipeline only ever added/updated `job_postings` rows, never marked anything closed, so the DB would grow without bound and filled/pulled roles would stay searchable forever. Fixed before starting Phase 5 rather than deferred:
- [x] `job_postings.status` (`active`/`closed`, default `active`) — soft-delete, not a hard `DELETE`, since `applications.job_posting_id` references these rows and a user's application history needs the title/company/url to keep resolving even after the posting closes
- [x] `lib/ingestion/run.ts` closes any posting that was active last run but wasn't in this run's fetch (`determineClosedExternalIds`, unit-tested in isolation), and explicitly re-activates any posting that reappears — only runs after a successful fetch, so a transient network failure can never be mistaken for "this company now has zero open roles" and wrongly close everything
- [x] Both `match_job_postings` and `match_jobs_for_resume` RPCs, plus the `/dashboard/jobs` search query, filter to `status = 'active'`
- [x] e2e coverage: extended the idempotency test to assert a re-fetched posting stays active, and added a new test that inserts a fake posting, runs ingestion, and confirms it gets marked closed (a real ATS's live postings can't be reliably made to "disappear" on demand for a test, so this uses a controlled fixture instead of live data for this one assertion)
- [ ] **Not done here, folded into Phase 7 instead:** removing a closed posting from a user's saved/applied board and notifying them — that needs the tracker UI to exist first (see Phase 7 below)

**Outstanding from Phase 1, not blocking anything but not forgotten:** Google OAuth isn't configured (deferred by choice). `main`-branch protection (blocking direct pushes, requiring PRs) has been requested by the user but is **not yet confirmed set up** — it requires a GitHub ruleset change neither `gh` CLI nor an API token is available here to make; needs manual verification.

## Immediate Backlog

**Phase 0 — Project setup** ✅ done 2026-07-24, ✅ verified live 2026-07-25, ✅ merged to `main` 2026-07-25 (PR #5)

**Phase 1 — Core MVP (auth + storage)** ✅ built + merged to `main` 2026-07-25 (PR #5)
- [x] Supabase Auth (email + Google OAuth) integration — email/password ready; Google provider deliberately not configured yet (deferred, not blocking)
- [x] Resume CRUD with free-tier limit (3), edited as rich text (Tiptap), not raw file upload
- [x] Cover letter CRUD with free-tier limit (1), same editor
- [x] Basic dashboard UI
- [x] `.docx` export for both document types
- [ ] **Deferred by design:** `.docx` *import* (upload an existing resume to start editing) — user chose create-in-editor-only for Phase 1; revisit if users want to start from an existing file
- [x] Signup and login confirmed working in the browser
- [x] Diagnosed and fixed a `23503` foreign-key error on document creation (missing `profiles` row for the user's account — see session log)

**Phase 2 — AI features** ✅ built + e2e browser-tested + polished + merged to `main` (PR #6)
- [x] Gemini-powered resume tailoring to a pasted job description — creates a new non-base (`is_base: false`) copy linked via `base_resume_id`; doesn't touch the original, doesn't count against the resume limit
- [x] Gemini-powered cover letter generation from a selected resume + pasted job description — fills the free base cover letter first, further generations are non-counting tailored copies
- [x] `e2e/ai-features.spec.ts`: real Playwright test covering login → create resume → type content → autosave → tailor for a job → generate cover letter, against live Supabase + Gemini, self-cleaning test user
- [x] Fixed and e2e-verified: tailored/AI-generated documents were unreachable after creation (`listBaseDocuments` only shows `is_base: true`) — added `listTailoredDocuments` + a "Tailored versions" list on each base document's editor page, and made cover letter generation set `base_cover_letter_id` (it previously set no parent link at all)
- [x] ~~no per-user rate limiting on AI generation~~ — resolved in Phase 6 (`lib/aiUsage/`)
- [ ] **Carried forward:** `.docx` export has no e2e coverage (see Phase 3, now merged — still not covered), no custom `error.tsx`/`loading.tsx` (see Phase 8, front-end polish)

**Phase 3 — Job board aggregation** ✅ built + e2e-tested + merged to `main` (PR #7)
- [x] Greenhouse/Lever/Ashby/Workable ingestion — one connector per ATS (`lib/ingestion/sources/`), no auth required for any of them; unit-tested against real captured response shapes, not guessed ones
- [x] Seed company list (`lib/ingestion/companies.ts`) — every token live-verified via direct API call before being added, since there's no discovery API for any of these ATSs
- [x] Normalization + dedup pipeline: exact `dedup_key` match (indexed) first, embedding similarity within the same company (`match_job_postings` pgvector RPC) as fallback
- [x] Cron route (`/api/cron/ingest`, `CRON_SECRET`-gated) + `vercel.json` schedule (every 6 hours, not live until Phase 10 deployment)
- [x] Job search/browse UI (`/dashboard/jobs`) — text + remote filter, user input sanitized before building the PostgREST filter string
- [x] `e2e/job-ingestion.spec.ts`: real pipeline run (fetch → embed → dedup → upsert) against live Ashby/Linear, proves idempotency (re-run doesn't duplicate rows), proves results reach the search UI
- [x] Unit tests for all 4 connectors' normalization logic (mocked fetch, real fixture data)
- [x] Found + fixed **three** real bugs, not hypotheticals (see session log): `embed_content`'s ~100/min rate limit (batching + `retryOnRateLimit`); `lib/supabase/server.ts` being unimportable from plain Node contexts because it bundled a `next/headers`-dependent export with a plain one (split into `serviceRole.ts`); and a full-scale timing run against real data surfaced 10/104 Vanta postings failing on a `NOT NULL` constraint because Ashby's `isRemote` field is sometimes absent, not always the boolean its docs imply — a hand-written fixture had missed this because it only ever set the field to `true`
- [x] **Production-scale ingestion timed for real** (carried-forward item, now resolved): a full run across all 7 seed companies took **65–107s** for 237–442 postings (variance from transient per-company fetch failures, not from the ingestion logic — see session log). This comfortably fits the `vercel.json` `maxDuration: 300` config, but **exceeds Vercel's Hobby plan's hard 60s cap regardless of `maxDuration`** — confirms a paid Vercel plan (or splitting ingestion into per-company invocations) is a real Phase 9 requirement, not a hypothetical one
- [x] Added a lightweight retry for transient ATS fetch failures (`fetch failed` / DNS blips) — observed live against multiple sources during testing, not just once
- [ ] **Carried forward:** `.docx` export e2e coverage (from Phase 2) — still not done, low priority (pure download, not stateful)

**Phase 4 — Job-to-resume match scoring** (branch: `phase-4`) ✅ built + e2e-tested
Split out of Phase 3 by explicit choice (real job postings now exist to test against, keeping this phase focused). Proceeds using each resume's own embedding, as originally planned — does *not* wait on Phase 5's career profile.
- [x] Score a resume against job postings — **not** `match_job_postings` as the backlog originally assumed (see session log: that RPC is company-scoped with a 0.85 near-duplicate threshold, wrong shape for this). Added a new `match_jobs_for_resume` RPC instead (`supabase/migrations/0003_match_scoring.sql`): searches across all companies, 0.3 similarity floor, one result per `dedup_group_id`.
- [x] Surface match scores in the UI — landed on a "Matching jobs" panel on each resume's editor page (`components/editor/MatchingJobsPanel.tsx`), not the job search page — a per-resume view fit the existing architecture (each resume already owns its own page) more directly than picking "which resume" contextually on `/dashboard/jobs`.
- [x] Resume embeddings computed lazily on click (`lib/matching/actions.ts`), not on every autosave — cached on `resumes.embedding` with a content hash (`embedding_source_hash`) so a repeat click skips re-embedding unchanged content.

**Phase 5 — Career profile (projects, education, work history)** ✅ built + e2e-tested
Added to the roadmap 2026-07-26. Lets Gemini draw on a fuller picture of the candidate than whatever's in one resume — both for Phase 2's tailoring (deciding *which* projects/experience to feature per job posting) and for improving Phase 4's match scoring later. Scoped to come after Phase 4, not before or folded into it, by explicit choice.
- [x] **Schema** (`supabase/migrations/0005_career_profile.sql`): `projects` (title, description, tech_stack, github_url nullable), `education` (institution, degree, field_of_study, start/end date, description), `work_experience` (company, title, location, start/end date — null end = current, description). All `on delete cascade` from `profiles`, RLS owner-only (same pattern as `resumes`/`cover_letters`).
- [x] **Tier limit on projects**: 3 free / 25 pro, matching the resume limit — landed on a parallel `PROJECT_LIMITS` helper in `lib/projects/db.ts` rather than reusing `lib/documents/db.ts`'s `BASE_LIMITS`, since projects don't share resumes/cover-letters' base/tailored-document model (no `is_base` concept). Education and work history are **not** tier-limited, as scoped.
- [x] **GitHub README summarization** (`lib/github/fetchRepo.ts` + `lib/gemini/summarizeProject.ts`): fetches repo metadata + README via GitHub's public API (unauthenticated, ~60 req/hour — 404/403/network failures all get distinct, specific error messages rather than a generic failure), passes it to Gemini **once** at project-creation time (`lib/projects/actions.ts`'s `createProject`) to generate an editable `description` + `techStack`. Not re-read on any later tailoring/scoring call, as scoped.
- [x] **Profile UI** (`/dashboard/profile`, `components/profile/*Section.tsx`): three sections (Projects, Education, Work Experience), each with an add form and an inline `<details>`-based edit/delete per item — reusing the collapsible-panel pattern already established for "Tailor for a job" rather than building dedicated per-item edit pages, since these are plain structured fields, not rich text.
- [x] **Wired into resume tailoring**: `lib/gemini/tailorResume.ts` now accepts an optional `careerProfile` text block (built by `lib/profile/formatForPrompt.ts`), passed in by `lib/resumes/actions.ts`'s `tailorResume` action, with explicit instruction that Gemini may pull in a relevant profile item not present in the resume being tailored.
- [x] e2e test (`e2e/career-profile.spec.ts`): adds a project via a real GitHub URL (octocat/Hello-World) and confirms the Gemini-generated description appears and is editable; adds a second, manually-described project with a deliberately distinctive skill set (a fictional "Quantum Widget Scheduler" project, Rust/gRPC); tailors a resume that never mentions it against a tightly-matched job description, and asserts the tailored output actually references it — proving the profile data influences tailoring, not just that the button doesn't error.

**Phase 6 — Billing** ✅ built + e2e-tested
Built against a real Stripe test-mode account (user set one up 2026-07-27 specifically for this phase — test mode is free, no real charges, so there was no reason to defer it). Pro price is a placeholder $9.99/month (already existed on the user's Stripe product before this phase started) — easy to change later in the Stripe dashboard with no code change.
- [x] **Stripe checkout + customer portal** (`lib/stripe/actions.ts`): `createCheckoutSession` reuses an existing Stripe customer if the user has one (from a prior canceled subscription) rather than creating a new one every time; `createPortalSession` opens the Stripe-hosted billing portal. New `/dashboard/billing` page (current plan, upgrade/manage buttons).
- [x] **Webhook handling** (`app/api/webhooks/stripe/route.ts`): `checkout.session.completed`, `customer.subscription.created/updated/deleted` all sync into a new `subscriptions` table and `profiles.subscription_tier` (a status mapping — `active`/`trialing` → pro, everything else → free — not a one-way "cancel sets free" flag, so resubscribing or payment recovery work the same way). Fixed `lib/stripe/client.ts` to the lazy-singleton pattern (`lib/gemini/client.ts`'s `getGeminiClient()` convention) before building on it — it was still doing Phase-0-era top-level construction.
- [x] **Real Stripe CLI e2e test** (`e2e/billing.spec.ts`), not just a unit test of the handler: spawns `stripe listen --forward-to localhost:3000/api/webhooks/stripe`, uses `stripe trigger customer.subscription.created/deleted --override subscription:metadata[user_id]=<id>` to fire real (test-mode) webhook events for a real throwaway user, and polls the DB for the tier flip in both directions. Installed the Stripe CLI via `winget install Stripe.StripeCli` — no interactive login needed, `--api-key` works directly.
- [x] Manually verified the full subscription lifecycle against the real webhook endpoint *before* writing the Playwright test (faster iteration), which caught a real bug: `profiles.stripe_customer_id` never got backfilled for a subscription that came from a webhook event not tied to our own checkout flow (exactly what `stripe trigger` produces, but also a real scenario — e.g. a subscription touched directly in the Stripe dashboard) — `createPortalSession` would have silently had no customer id to open a portal for. Fixed by having `upsertSubscription` set `stripe_customer_id` unconditionally, not just at checkout-creation time. Also confirmed via the installed SDK's own type definitions (not guessed) that `current_period_end` lives on each subscription *item* in this API version, not the top-level `Subscription` object — a real, non-obvious gotcha.
- [x] **Rate-limit AI generation calls per user** (carried from Phase 2, no longer just flagged): new `ai_generation_events` table (migration `0006_ai_rate_limit.sql`, rolling 24h window via a plain event log rather than a calendar-day counter), a placeholder `AI_GENERATION_LIMITS` (free: 10/day, pro: 100/day — easy to retune once real usage data exists), checked at the top of `tailorResume`, `generateCoverLetter`, and the project GitHub-summarization path (the three real `generate_content` call sites) before spending a Gemini call.
- [x] Downgrade/cancellation decision: existing over-limit documents stay fully accessible and editable, not forced read-only — only *new* base-document creation is blocked once at/over the tier's limit (already the existing behavior via `baseDocumentLimit`/`countBaseDocuments`, so no extra enforcement code was needed). Nothing is ever silently deleted. Considered but deliberately skipped forcing old-over-limit docs read-only: real added complexity (would need enforcement in `updateDocumentContent` too) for a corner case the original backlog note only called "likely," not required.

**Phase 7 — Application tracking** ✅ built + e2e-tested
- [x] Save/apply/status pipeline (saved → applied → interviewing → offer/rejected) — `applications` table already existed since `0001_init.sql`, no new migration needed for it. A "Save" button on `/dashboard/jobs` search results creates a `saved` application (a repeat click is a no-op, not a duplicate row); status/notes/linked-documents are edited on the new `/dashboard/applications` board.
- [x] Link applications to the specific resume/cover-letter version used — `listAllDocuments` (new, `lib/documents/db.ts`) lists *every* resume/cover letter a user has, base and tailored alike, so the application picker can point at the exact tailored copy actually sent for that job, not just a base document.
- [x] Applications list/dashboard view (`/dashboard/applications`) — "Active" and "Closed postings" sections, each application an inline `<details>`-based edit form (status/resume/cover-letter/notes), same pattern as the Phase 5 profile sections.
- [x] **Removal-from-board + notification when a linked posting closes** (added 2026-07-26 as a user request, scoped in full now): `lib/ingestion/run.ts`'s existing closure detection now also looks up any applications pointing at a just-closed posting and creates an in-app notification for each affected user (new `notifications` table, migration `0007_notifications.sql`, in-app only per the user's earlier choice — no email/SMTP dependency). The closed application itself stays in the user's history (status/notes/resume link intact) — just moved from "Active" to "Closed postings" on the board, never deleted. A `NotificationBell` in the dashboard header surfaces unread notifications with a dismiss action.

**Phase 8 — STEM job board pivot (originally "front-end visual polish")** 🚧 in progress
Promoted to its own phase 2026-07-27 at the user's explicit request (previously a single flagged bullet under Hardening, added 2026-07-27, not yet scoped). Current UI had been built functionality-first across every phase so far — plain Tailwind utility classes, default spacing/type scale, no real visual design system. User chose shadcn/ui over refined custom Tailwind, and a distinct brand identity over staying minimal/grayscale.

**2026-07-31 — scope redefined mid-phase, before merge:** the user asked to "fully restart" the front-end and, in the same request, redefine the product itself — job search becomes the app's primary/hero surface (a real STEM job board, categorized by discipline) instead of one dashboard tab among six, with AI resume/cover-letter tailoring staying a strong feature but now reachable *from* a listing instead of being the thing users sign up for first. Locked decisions from an explicit user Q&A: job search stays behind login (no public browsing — the landing page still has to sell signup on the search pitch); categorization must be a real persisted/filterable system, not cosmetic framing; the visual identity gets rebuilt from scratch rather than reskinned (the coral direction below was explicitly rejected for this reason alone, independent of the IA change). Work done under the new scope, all on `phase-8`, not yet merged:
- [x] **STEM discipline categorization** — `lib/ingestion/categorize.ts` (pure, unit-tested, 7 buckets), `job_postings.category` (`0008_job_category.sql`), department-hint wiring for Lever (`categories.team`) and Greenhouse (`departments[]` — confirmed live 2026-07-30 against a real board, wasn't even declared in the connector's TS interface before this); Ashby/Workable have no structured signal, keyword-only. Existing postings self-backfill on the next ingestion run (every currently-fetched posting gets re-upserted every pass) — no manual script needed.
- [x] **Seed company list expanded 7 → 22**, spanning software/data-ML/biotech/hardware so category filters aren't dominated by one bucket — every new token live-curl-verified before being added, several plausible candidates tried and rejected (404s) rather than guessed in.
- [x] **Job listing → AI tailoring bridge** — `tailorResume`/`generateCoverLetter` accept an optional `jobPostingId`, source the description server-side, and auto-save the posting to `applications` with the new document linked, reusing the existing job+document co-location there instead of a parallel mechanism. New `/dashboard/jobs/[id]` detail page carries this; `MatchingJobsPanel` links straight into it per match.
- [x] **Dashboard IA restructure** — `/dashboard` now redirects to `/dashboard/jobs` (the old resume/cover-letter-count overview moved into a compact `AccountSummaryStrip` above the search form); nav reordered to lead with Job Search/Applications. Updated the 5 e2e specs that asserted the old post-login `/dashboard` destination.
- [x] **Second full visual-identity rebuild** — "engineering instrument panel": cool graphite neutrals + a signal-blue accent (distinct from both the original indigo and the coral direction immediately below), tighter `--radius`, self-hosted IBM Plex Sans/Mono replacing Plus Jakarta Sans. Landing page hero rebuilt around a static preview of real categorized search results instead of a generic icon grid; copy repositioned to lead with STEM search. `/dashboard/jobs` restructured into a filter-sidebar + dense-row layout.
- [x] Also fixed along the way (unrelated to the pivot itself, found via a `web-design-guidelines` skill review before the pivot conversation started): destructive-action confirmation dialogs (none of the app's 6 delete flows had one), missing `color-scheme` alongside the `.dark` class (native `<select>`s rendered with light OS chrome in dark mode), missing skip-to-content link, landing page nesting `<header>` inside `<main>`.
- [ ] **Not yet done**: full `npm run test:e2e` run against the pivot's new/updated specs (new category-filter and job-tailoring-bridge tests, updated `waitForURL` assertions) — see Verification below before this phase is ready for review.

**Original scope, built before the 2026-07-31 pivot (still in place, superseded only where noted above):**
- [x] **Component library**: shadcn/ui initialized (`components.json`, `components/ui/*`) — this particular install uses Base UI primitives under the hood (not Radix), the "base" style. Adopted for `Button`, `Input`, `Textarea`, `Card`, `Badge`, `Label`, `Separator`, `Alert`, `Skeleton` everywhere. Deliberately did **not** swap native `<select>`/`<input type="checkbox">`/`<details>` for shadcn's `Select`/`Checkbox`/`Collapsible` — those are custom Base UI widgets, not native-element passthroughs, and the app's entire e2e suite depends on plain `<form action={serverAction}>` + `FormData` submission working exactly as-is; native elements achieve the same visual consistency (styled to match) with zero risk to every existing tested flow. `DropdownMenu` was tried for `NotificationBell` and dropped for the same reason — its Menu primitive doesn't nest per-item `<form>`s cleanly.
- [x] **Brand identity** (revised 2026-07-28): first pass used a cool indigo accent — user feedback was that it "looks as if someone just began to code," wanting something more welcoming (referenced builtin.com). Replaced with a warm coral/orange-red accent (oklch hue ~38) against warm off-white/charcoal neutrals (a faint warm tint, not stark gray/white) — one deliberate accent color, defined once in `app/globals.css`'s theme tokens (`--primary`, `--ring`, `--accent`, `--chart-*`, light + dark), kept clearly distinct in hue from `--destructive` (true red) so a delete button and a primary CTA never read as the same color. `--radius` also bumped from `0.625rem` to `1rem` and `Card` given a soft shadow instead of a thin ring, for a friendlier, less corporate feel.
- [x] **Dark mode toggle** (added 2026-07-28, user request): previously the app only had `dark:` Tailwind variants with no way to actually switch into dark mode — the class-based dark variant (`.dark`) was never applied by anything. Added a real toggle (`components/theme/ThemeToggle.tsx`, sun/moon icon) that flips a `dark` class on `<html>` and persists to `localStorage`, plus a synchronous inline script in the root layout that applies the stored (or OS-default) theme before first paint, so switching to dark mode doesn't flash light on every navigation.
- [x] Landing page (`app/page.tsx`) rebuilt with a real hero + colorful per-feature icon chips, plus a shared `SiteHeader` (logo, sign in/get started, theme toggle) also used on login/signup/check-email for a consistent marketing-page shell.
- [x] `loading.tsx`/`error.tsx` added at both root and `app/dashboard/` (Skeleton-based loading state, Alert-based error boundary with a retry button) — closes the carried-forward Phase 2 gap.
- [x] Full pass over every page: landing, login/signup/check-email, dashboard shell (nav + `NotificationBell`), dashboard overview, resumes/cover-letters (list + editor + toolbar + AI panels), jobs search, applications board, profile sections, billing.
- [x] Full e2e suite re-verified after the rewrite (see below) — caught and fixed a real markup bug in the process, not just a styling risk that didn't materialize.
- [x] Visually self-verified before calling it done: screenshotted the landing and login pages in both light and dark mode via a throwaway Playwright script rather than just trusting the build succeeded — actually looked at the rendered output before reporting this as complete.
- [x] **Typeface + legibility revision** (2026-07-30, user feedback): user reported both a build error and that the font "looks... plain... like an email or essay." The two turned out to be connected: `next/font/google`'s Geist fetch is unreliable in this environment (already documented once this session for the dev-server font warning) and was silently falling back to the browser default serif — which is exactly what "looks like an essay" describes. Switched to a **self-hosted** typeface (Plus Jakarta Sans variable woff2, `app/fonts/`, loaded via `next/font/local`) specifically to remove the network dependency at build/runtime entirely, and dropped `Geist_Mono` for a plain system monospace stack (the only mono usage is incidental Tiptap code-block styling, not worth a second font fetch). Chosen for real character — geometric, warm, distinctly not a generic default — consistent with the builtin.com-style direction from the earlier revision.
  Investigating this surfaced a second, independent, and more fundamental bug: the font loader's CSS variable was applied via `className` on `<body>`, while `globals.css` applies the `font-sans` utility on `<html>` — and CSS custom properties only cascade **downward** from ancestor to descendant, never back up to a parent. `<html>` genuinely could not see a variable defined on its own child `<body>`, so **no font ever actually applied, regardless of which one was configured** — this was true for the original Geist setup too, independent of whether the Google Fonts fetch succeeded. Fixed by moving the font's `className` onto `<html>` instead. Confirmed the fix visually (not just via the build passing): re-screenshotted and could clearly see real Plus Jakarta Sans letterforms in both themes, not the serif fallback.
  Also addressed the separate "everything seems a little small" legibility complaint: bumped the root `html` font-size from the 16px browser default to 18px (scales every rem-based Tailwind utility — text sizes, padding, gaps — proportionally in one change, rather than hunting down each `text-sm` usage), removed shadcn's `md:text-sm` mobile-zoom-prevention downsize on `Input`/`Textarea` (was shrinking text back down on desktop, working against the goal), and bumped `Button`/`Input`/`Textarea`/`Label` default heights and text size up a step. Re-verified the full testing gate after: `lint`/`tsc`/`test` (41)/`build`/`test:e2e` (9/9, one transient Supabase auth fetch failure on the full run that passed cleanly in isolation) all pass.

**Phase 9 — Pre-launch hardening**
Not a feature phase — this is what stands between "works on my machine" and "safe to point real users at."
- [x] **Upgrade the Gemini API key to a paid tier** — done 2026-07-26; removed the free tier's 20-requests/day hard cap that had blocked `ai-features.spec.ts`. Per-user cost control was a separate, still-open item at the time — resolved in Phase 6 (`lib/aiUsage/`).
- [ ] **Decide the production Vercel plan tier (or split the cron job)** — Phase 3 timed a real full-scale ingestion run at 65–107s, which exceeds Vercel Hobby's hard 60s function cap regardless of `maxDuration` config. Needs either a Pro-or-higher plan or restructuring `/api/cron/ingest` into one invocation per company.
- [ ] **Production Supabase project**, separate from the current dev one; a migration promotion process (`supabase/migrations/` already exists — decide CLI-driven vs. manual SQL-editor promotion)
- [ ] **Custom SMTP for Supabase Auth emails** — the default provider has very low send limits, not viable for real signups (confirmation emails, password resets)
- [ ] **CI**: GitHub Actions workflow running `lint` + `tsc --noEmit` + `test` + `build` on every PR, ideally `test:e2e` too against a disposable/staging Supabase project — turns the "testing gate" in `CLAUDE.md` from a manually-enforced habit into something structurally guaranteed. Needs its own paid Gemini key (separate usage from local dev).
- [ ] **Legal pages**: Privacy Policy + Terms of Service — required before collecting payments and resume/PII data from real users
- [ ] **Error monitoring** in production (Sentry or Vercel's built-in) — right now a production error is invisible until a user reports it
- [ ] **Rate limiting / abuse prevention** more broadly than just AI calls — auth endpoints (brute force), `.docx` export (cheap but still a DB read per request)
- [ ] Accessibility pass: keyboard navigation through the editor and forms, focus states, labels on any icon-only controls
- [ ] Basic SEO: per-page metadata, `sitemap.xml`, `robots.txt`, an OG image for the landing page
- [ ] Confirm Supabase backup/point-in-time recovery is available on whatever plan the production project ends up on
- [ ] **Move Stripe from test mode to live mode** (Phase 6 built against test mode only) — new live API keys, live webhook secret/endpoint, live price ID; re-verify checkout/webhook flow against real Stripe infrastructure before real charges are possible.

**Phase 10 — Launch**
- [ ] Production domain + Vercel deployment, environment variables promoted from dev → prod (live Stripe keys, paid Gemini key, production Supabase keys)
- [ ] Google OAuth actually configured (deferred since Phase 1 — needs a Google Cloud OAuth client tied to the production domain, not localhost)
- [ ] Re-enable the GitHub `main`-branch ruleset requirements if they were ever relaxed for any reason (see the "not yet confirmed set up" note above — resolve that before this point, not after)
- [ ] Soft launch / beta invite flow before a fully public signup, if desired

## Recent Session Activity
- **2026-07-24** — Initial onboarding audit; created `CLAUDE.md`, `.claudeignore`, `PROGRESS.md` for the bare scaffold.
- **2026-07-24** — User corrected scope: JobLens is an AI job-search assistant (Gemini-powered resume/cover-letter tailoring, application tracking, deduplicated multi-source job board), not a generic template. Researched job-data access (Indeed API retired 2024, no LinkedIn public API — scraping ruled out on ToS/legal grounds), AI SaaS stack conventions, and Gemini SDK. User chose: free ATS APIs (Greenhouse/Lever/Ashby/Workable) over scraping/paid aggregator for now, Supabase over best-of-breed split, Stripe over Lemon Squeezy. Rewrote `CLAUDE.md` and this backlog to match. No code written yet.
- **2026-07-24** — Completed Phase 0. Installed `@supabase/supabase-js`, `@supabase/ssr`, `@google/genai`, `stripe`, `zod` + Vitest/Playwright/Testing Library dev deps; patched a critical Next.js RCE by bumping 15.5.0→15.5.21 (left a handful of dev-only ESLint-chain ReDoS advisories alone since npm's fix would downgrade `@eslint/eslintrc` and break linting). Wrote the full DB schema (`supabase/migrations/0001_init.sql`) with RLS on every user-owned table and a pgvector column on `job_postings` for dedup/matching. Added `lib/supabase/{client,server,middleware}.ts`, root `middleware.ts` for session refresh, `lib/gemini/client.ts`, `lib/stripe/client.ts`, `lib/ingestion/normalize.ts` (dedup key builder, with tests), and `types/database.ts`. Set `"type": "module"` in `package.json` and defaulted the Vitest environment to `node` (not `jsdom`) to work around a jsdom transitive-dependency ESM/CJS bug — component tests should opt into jsdom per-file. Verified `lint`, `tsc --noEmit`, and `test` all pass. Updated `README.md` and `app/layout.tsx` metadata. Supabase/Stripe/Gemini accounts and the actual migration run are still outstanding — need the user's own credentials.
- **2026-07-24** — Force-pushed the local JobLens rebuild over the old GitHub `main` (previous React-based attempt with login/dashboard/landing pages), per user's explicit choice to erase rather than preserve that history. Required the user to temporarily disable the "block force pushes" rule in the repo's GitHub ruleset first — no `gh` CLI available in this environment to do it via API.
- **2026-07-25** — Built Phase 1. User added a requirement mid-phase: resumes/cover letters should be editable as a Word-style document in-browser, not just uploaded files — researched editor options (Tiptap recommended over Lexical for this use case) and confirmed with the user to scope out `.docx` import for now (create-in-editor only, export-to-`.docx` via the `docx` package). Built: email/password + Google OAuth via Supabase Auth (`lib/auth/actions.ts`, `app/login`, `app/signup`, `app/auth/callback`), protected dashboard shell, resume/cover-letter list+create+delete pages with tier-limit enforcement, Tiptap editor with 1s-debounced autosave (`components/editor/`), `.docx` export routes, and the Tiptap→docx converter (`lib/tiptap/toDocx.ts`, with tests). Landing page and default create-next-app SVGs replaced/removed. Hit and fixed two non-obvious issues: (1) a hand-written Supabase `Database` type silently resolves all query results to `never` unless it includes `Views`/`Functions`/`Enums`/`CompositeTypes` and every table has `Relationships: []` — not optional, even when unused; (2) `NextResponse` needs a `Uint8Array`, not a raw Node `Buffer`, for a binary body. `lint`, `tsc --noEmit`, `test`, and `build` all pass. Still not runtime-verified — no live Supabase project yet (same gap as Phase 0).
- **2026-07-25** — User created the live Supabase project and ran the migration; wired up `.env.local` (gitignored, values never echoed back after the initial paste). Caught and fixed two real bugs during connection testing: (1) the migration never enabled RLS on `job_sources`, so its 4 seeded rows were invisible to the anon key despite existing in the database (diagnosed via a `pg_catalog` query comparing live DB state to expectations, then patched both the live DB and `supabase/migrations/0001_init.sql`); (2) `getDocument()` threw an unhandled Postgres `22P02 invalid input syntax for type uuid` error (→ HTTP 500) for any non-UUID document ID instead of a clean 404 — found by deliberately hitting the export route with a bogus ID, fixed by catching that error code and treating it as "not found." Started the dev server and confirmed `/`, `/login`, `/signup` return 200, `/dashboard` and its subroutes correctly 307-redirect unauthenticated requests to `/login`, and both malformed and valid-but-missing document IDs now 404 instead of 500. No one has clicked through the actual UI in a browser yet — only route-level HTTP checks so far. Dev server left running for the user to try next.
- **2026-07-25** — User confirmed signup and login work in the browser. Committed all Phase 0 + Phase 1 work (34 files) to the pre-existing `phase-1` branch and pushed it to GitHub — nothing was committed straight to `main`. Drafted a PR description (summary, bugs found, explicitly-out-of-scope, testing performed/outstanding) since no `gh` CLI is available in this environment to open the PR directly; gave the user the compare URL and copy-pasteable title/body instead. Added a standing "PR Workflow" section to `CLAUDE.md` so this happens automatically at the end of every phase from now on, without being asked.
- **2026-07-25** — User hit a `23503` foreign-key-violation error creating a cover letter. Diagnosed via the live DB (using the service role key, no `gh`/psql access needed): `public.profiles` was completely empty despite the user having a real, confirmed `auth.users` account — the `on_auth_user_created` trigger hadn't fired for that specific signup. Verified the trigger definition itself is correct by creating a throwaway test user via the Supabase Auth admin API: a profile row was created for it automatically, proving the trigger works for new signups (then deleted the test user; `ON DELETE CASCADE` cleaned up its profile too). Manually backfilled the missing `profiles` row for the real account via a direct `POST` to the PostgREST API with the service role key — no code or migration changes needed, this was a one-off data gap, most likely a timing fluke from right after the migration/trigger were first created. User should retry creating a resume/cover letter now.
- **2026-07-25** — User merged and deleted `phase-1` on GitHub (PR #5). Did the branch handoff: `git checkout main && git pull` (fast-forwarded onto the merge commit), `git fetch --prune` (cleared the stale `origin/phase-1` ref), `git branch -d phase-1` (local delete, safe because it was fully merged), then created and pushed `phase-2`. Codified this whole sequence into `CLAUDE.md`'s "PR Workflow" section as a "starting a new phase" checklist to run automatically going forward, no longer just an "ending a phase" one. User also asked that `main` be made un-editable except via merged PRs — documented in `CLAUDE.md` that this needs a GitHub ruleset change (Settings → Rules → Rulesets) which has to be done manually in the browser; flagged as not-yet-confirmed-active rather than assumed done.
- **2026-07-25** — Built Phase 2. Scoped down first: job description input is a paste-in text box (not tied to Phase 3 job postings), and job-to-resume match scoring was moved into Phase 3 outright, since scoring needs a real corpus of postings to be meaningful. Designed a flat `Block[]` shape (`lib/gemini/schemas.ts`) as the AI response format instead of raw Tiptap JSON — simpler for Gemini to generate reliably, converted to/from real editor content via new `lib/tiptap/toPlainText.ts` and `fromBlocks.ts` (both unit-tested). Wired resume tailoring (`lib/gemini/tailorResume.ts`) and cover letter generation (`lib/gemini/generateCoverLetter.ts`) through Gemini's `responseJsonSchema` structured output, Zod-validated on the way back. Extended `lib/documents/db.ts` with a general `insertDocument` so AI-generated documents can be created as non-base (tailored, non-counting) or as someone's first base document, reusing the existing tier-limit machinery rather than adding new rules. Added UI: a collapsible "Tailor for a job" panel on the resume editor, a "Generate with AI" panel on the cover letters list page. Hit a real build hang (production build stalled indefinitely after "Environments: .env.local") caused by orphaned Node processes from an earlier killed dev server holding a Windows file lock on `.next/trace` — not a code bug; killing the stray `node.exe` processes and clearing `.next` fixed it. Separately noticed (and fixed) that `lib/gemini/client.ts` was constructing the `GoogleGenAI` client at module top-level, which gets evaluated at build time for every page that transitively imports it via Server Actions — caused 8 "API key should be set" build warnings even on pages that never touch Gemini; switched to a lazy `getGeminiClient()` singleton. `lint`, `tsc --noEmit`, `test`, and `build` all pass clean. Not runtime-tested — no `GEMINI_API_KEY` yet.
- **2026-07-25** — User provided a real `GEMINI_API_KEY`, added to `.env.local` (never echoed back in chat). A direct API call to `generateContent` with model `gemini-2.5-flash` (the constant already in the codebase) failed with a 404 "no longer available to new users" — confirmed via the `ListModels` endpoint that the model is still *listed* as available but not actually callable by this key, and that `text-embedding-004` (the embedding constant, unused until Phase 3) isn't listed at all. Switched both to Google's rolling aliases instead of pinned snapshots — `gemini-flash-latest` and `gemini-embedding-001` — specifically to avoid this same failure mode recurring as models rotate; documented the reasoning directly in `lib/gemini/client.ts` and `CLAUDE.md`. Verified the real production code paths end-to-end with a temporary, uncommitted Vitest file that called `tailorResumeContent` and `generateCoverLetterContent` directly against the live API (not just a hand-rolled smoke script) — both produced correctly-structured, grounded output with no fabricated experience, then the test file was deleted rather than committed (don't want CI depending on a live key/network/cost). Re-ran `lint`/`tsc`/`test`/`build` clean after the model-constant change.
- **2026-07-25** — User asked to actually test the AI features in the browser before moving on, and to always require testing before any merge to `main` going forward. Rather than manual click-through, built a real Playwright e2e test (`e2e/ai-features.spec.ts`) that logs in, creates a resume, types content, waits for autosave, tailors it for a job via the real "Tailor for a job" UI, then generates a cover letter via the real "Generate with AI" UI — all against the live dev server, live Supabase, and live Gemini. The test creates its own throwaway user via the Supabase admin API and deletes it in `afterAll`; verified after the run that cleanup left no orphaned rows (only found one unrelated leftover row from the user's own earlier manual testing, not from this test). Installed Playwright's Chromium binary (`npx playwright install chromium`, needed to run via Bash since the network download fails from the PowerShell sandbox, same restriction as before) and added `.env.local` loading to `playwright.config.ts` via `process.loadEnvFile()` so e2e tests can hit Supabase directly. Hit and worked around a OneDrive-specific issue: this project directory being inside OneDrive sync caused `EPERM: operation not permitted, rmdir` on Playwright's `test-results/` cleanup between runs — not a real test failure, fixed by manually clearing `test-results`/`playwright-report` first; documented this in `CLAUDE.md` so it doesn't get mistaken for a real bug again. Added a `data-testid="document-title"` to the editor's title input for reliable test targeting. Test passed cleanly (2/2 including the existing home-page test). Added a "testing gate" to `CLAUDE.md`'s PR Workflow: real Playwright e2e coverage (not just unit tests) is now required before merging anything user-facing, with judgment left for pure-logic changes. Re-ran the full suite (`lint`/`tsc`/`test`/`build`/`test:e2e`) clean.
- **2026-07-25** — User asked to build out the roadmap through deployment and determine whether Phase 2 needs more polish before moving on. Audited Phase 2 for real gaps rather than assuming it was done: found that `listBaseDocuments` (used by both list pages) only shows `is_base: true` rows, meaning every tailored resume and every AI-generated cover letter beyond the first became permanently unreachable the moment the user navigated away from the redirect that created it — no list, no link, nothing. Also found `generateCoverLetter` set no `base_cover_letter_id` at all on tailored cover letters, so even a fix to the list pages wouldn't have surfaced them. Fixed both: added `listTailoredDocuments(table, parentColumn, parentId)` to `lib/documents/db.ts`, a `TailoredVersionsList` component rendered on both editor pages, and updated `generateCoverLetter` to look up the user's existing base cover letter and link new tailored ones to it. Extended `e2e/ai-features.spec.ts` to prove the fix rather than just asserting it compiles. Rebuilt the Immediate Backlog into a full roadmap through Phase 7 (later renumbered to Phase 8 — see below). **Determination: Phase 2 is ready to merge, move to Phase 3.**
- **2026-07-25** — User manually merged Phase 2 (PR #6) and asked to proceed to Phase 3. Verified the merge actually landed on `origin/main` before touching branches (a transient DNS blip made the first `git fetch` look like it hadn't). Ran the phase-start checklist: pulled `main`, pruned `phase-2`, created and pushed `phase-3`. Before building, asked whether Phase 3 should include job-to-resume match scoring or split it out — user chose to split it out, so it became its own **Phase 4** (renumbering Billing/Application tracking/Hardening/Launch to 5/6/7/8).
- **2026-07-25** — Researched the 4 target ATS APIs live before writing any connector code rather than guessing at response shapes: Greenhouse and Ashby both confirmed working with abundant real postings (Airbnb, Stripe, Linear, Vanta 104 jobs, Ramp 118 jobs); Lever and Workable endpoints confirmed functionally correct (valid responses) but every company slug tried happened to have zero current openings — used anyway since an ingestion pipeline has to handle "zero postings from a source" as a normal case regardless. Built all 4 connectors (`lib/ingestion/sources/`), each unit-tested against captured real (or, for Lever/Workable, documented-and-endpoint-verified) response fixtures — including a real gotcha caught this way: Greenhouse's `content` field is HTML-*encoded* (literal `&lt;div&gt;`), not raw HTML, so entities must be decoded before tags are stripped or the description ends up full of literal tag text. Built the dedup pipeline: a new migration (`0002_job_ingestion.sql`, applied live by the user) adds an indexed `dedup_key` column and a `match_job_postings` pgvector RPC (exact key match first, embedding similarity within the same company as fallback) — extended `types/database.ts`'s `Functions` map to type the RPC properly, having learned in Phase 1 that skipping this silently breaks query typing rather than erroring. Built the orchestrator (`lib/ingestion/run.ts`), the cron route (`CRON_SECRET`-gated), `vercel.json`, and a search UI (`/dashboard/jobs`) with sanitized search input (raw user text going into a PostgREST `.or()` filter string is a filter-injection risk, not just XSS).
- **2026-07-25** — e2e-testing the ingestion pipeline surfaced two real, non-hypothetical problems, both fixed: (1) `lib/ingestion/run.ts` imported `createServiceRoleClient` from `lib/supabase/server.ts`, which also exports a `next/headers`-dependent client — bundling them made the whole file unimportable from Playwright's plain-Node test process (`Cannot find module '.../next/headers'`); split the RLS-bypassing client into its own `lib/supabase/serviceRole.ts` with zero Next.js-runtime dependencies. (2) One embedding call per job posting hit Gemini's `embed_content` rate limit almost immediately once testing against a company with 25+ postings — fixed by batching (Gemini's `embedContent` accepts up to 100 texts per call, confirmed by testing the actual boundary live: 100 succeeds, 120 fails with an explicit "at most 100" error) and by adding `lib/gemini/retryOnRateLimit.ts`, which parses Google's own `retryDelay` out of the 429 response body and waits that long (confirmed values up to ~57s) rather than guessing with blind exponential backoff. Applied the same retry helper to `generateBlocks.ts` (used by Phase 2's resume tailoring/cover letter generation) for consistency. Also found and fixed that running e2e tests in parallel (Playwright's default) made separate test files compete for the same rate-limited quota simultaneously — switched to `fullyParallel: false` / `workers: 1`, with the reasoning documented in `playwright.config.ts` since it looks like a downgrade otherwise.
- **2026-07-25** — While re-verifying the full e2e suite, hit a *second*, different Gemini quota: `generate_content` on the free tier is capped at **20 requests/day** (not per-minute) — confirmed via the `quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier` field in the API's own error response. This session's cumulative manual verification calls plus repeated full-suite e2e runs exhausted it. Unlike the per-minute embedding limit, no amount of retrying within a test run fixes a per-day cap — confirmed this wasn't a code issue by directly calling `generateContent` outside any test and getting the same `RESOURCE_EXHAUSTED`/`PerDay` response. Documented this prominently in both `CLAUDE.md` (a dedicated "Gemini rate limits" section) and here so it isn't mistaken for a Phase 3 regression later, and added "upgrade to a paid Gemini tier" as a concrete Phase 7 pre-launch item rather than a vague "add rate limiting" placeholder. `ai-features.spec.ts` (unchanged Phase 2 functionality, which passed earlier in this exact session before the daily budget ran out) is the only test currently blocked by this; `job-ingestion.spec.ts`'s idempotency test successfully exercised and passed through the per-minute retry logic for real. Final verified state: `lint`/`tsc`/`test` (18)/`build` clean, e2e 3/4 (home, both job-ingestion tests) with the 4th blocked by exhausted daily quota, not a defect.
- **2026-07-25 (asked separately)** — User asked for a quick cost estimate before deciding whether to upgrade the Gemini key. Researched current published per-token rates rather than guessing: `gemini-embedding-001` at $0.15/1M input tokens, Gemini 3.5 Flash (closest documented proxy for whatever `gemini-flash-latest` currently resolves to) at $1.50/$9 per 1M input/output tokens. Worked out ~$0.007/tailor-or-generate action, ~$0.03/full ingestion run, landing on a ~$1–2/day estimate even for heavy testing volume. Framed the real takeaway correctly: cost isn't the free tier's problem, the hard 20/day request ceiling is — paid tier removes that ceiling regardless of token volume, it's not a "pick a plan" decision like most SaaS billing.
- **2026-07-26** — User confirmed the Gemini key is now paid tier and asked whether Phase 3 is actually ready to complete or needs more work. Re-ran the full e2e suite first rather than assuming: **4/4 passed**, including `ai-features.spec.ts` (previously blocked by the exhausted daily quota) — and the ingestion idempotency test's runtime dropped from ~46s–1.4min (rate-limit retry waits) to 15s, directly confirming the paid tier removed the bottleneck. Used the newly-unblocked budget productively rather than just declaring done: ran a real full-scale ingestion (all 7 seed companies, not just the single-company subset used to keep the e2e test cheap under the free tier) to get actual production timing data for the Phase 3 carry-forward item that needed it. That surfaced two more real, previously-hidden problems: (1) error messages inside `run.ts` were being formatted with `err instanceof Error ? err.message : String(err)`, which collapses Supabase's `PostgrestError` objects (real errors, but not `instanceof Error`) to the useless literal `"[object Object]"` — fixed with a proper `errorMessage()` helper that checks for a `message` property generically; (2) with real error messages visible, found that 10/104 live Vanta postings were failing a `NOT NULL` constraint on `job_postings.remote`, because Ashby's `isRemote` field is sometimes absent despite its docs implying it's always a boolean — the hand-written unit test fixture had missed this because it only ever set the field to `true`. Fixed the Ashby connector to default to `false`, added a regression test covering the missing-field case, and re-ran the full ingestion twice more to confirm zero errors. Also added a lightweight retry for transient ATS `fetch failed` errors (observed live against Airbnb, Stripe, and Plaid during these runs) at the orchestration level, matching the pattern already used for Gemini rate limits. Final confirmed numbers: 65–107s for 237–442 real postings across 7 companies — comfortably under the `vercel.json` `maxDuration: 300` budget, but over Vercel Hobby's hard 60s cap, confirming (not just flagging) that a paid Vercel plan or per-company cron splitting is a real Phase 7 requirement. Updated Phase 5/7's rate-limiting language to reflect that the paid tier makes this a cost-control problem now, not an availability one. Full suite re-verified clean: `lint`/`tsc`/`test` (19)/`build`/`test:e2e` (4/4). **Determination: Phase 3 is genuinely ready to merge** — the extra work done here wasn't "more Phase 3 build," it was using the paid-tier headroom to find and fix real bugs (the Vanta constraint violation, the swallowed error messages) that free-tier quota limits had been preventing from surfacing.
- **2026-07-26** — User said Phase 3 was merged and asked to continue to Phase 4. Local `main` was 3 commits behind `origin/main` (hadn't pulled since before the merge), which briefly made several files look like they'd reverted to their pre-merge state — checked `git log origin/main` before touching anything rather than assuming data loss; confirmed PR #7 (Phase 3) was cleanly merged with all commits intact, just not yet pulled locally. Ran the phase-start checklist (pull, prune, delete `phase-3`, create/push `phase-4`) and confirmed nothing was lost. Separately, user described a new feature: a profile-level "career library" (projects optionally linked to a GitHub repo, education, work history) that Gemini can draw on when tailoring resumes/cover letters, and asked to be consulted on open questions as they came up. Asked three: where it fits in the roadmap relative to Phase 4, how deep the GitHub integration should go, and the free-tier project limit. Landed on: Phase 4 (match scoring) proceeds as originally planned using each resume's own embedding; the profile feature becomes a new Phase 5 (renumbering Billing/Application tracking/Hardening/Launch to 6/7/8/9); GitHub READMEs get fetched and summarized by Gemini **once** at project-creation time into an editable description, not re-read on every subsequent tailoring/scoring call (the user's own framing — avoids repeatedly spending tokens on raw file content); 3 free projects, matching the resume limit, with education/work history left uncapped since they're a factual record rather than discretionary content. Wrote out Phase 5's full backlog (schema, GitHub summarization, profile UI, wiring into `tailorResume.ts` so the data doesn't sit unused) before returning to Phase 4.
- **2026-07-26** — Built Phase 4. Re-examined the backlog's assumption that `match_job_postings` (Phase 3's near-duplicate detector) could be "reused as-is" before writing any code — it can't: that RPC is hard-scoped to a single company and uses a 0.85 similarity floor tuned to catch reworded copies of the *same* posting, not "does this resume broadly fit this job" (a resume needs to search across every company, and 0.85 would return almost nothing). Added a new migration (`0003_match_scoring.sql`, applied live by the user) instead: `resumes.embedding` + `embedding_source_hash` columns, and a `match_jobs_for_resume` RPC (all companies, 0.3 threshold, `distinct on` dedup_group_id so near-duplicate postings don't crowd out results). Resume embeddings are computed lazily on click rather than on every autosave (`lib/matching/actions.ts`'s `findMatchingJobs` Server Action), cached with a content hash so a repeat click without edits skips the Gemini call entirely — deliberately keeping this an explicit user action, not automatic, given `CLAUDE.md`'s standing note that no per-user Gemini rate limiting exists yet. Added `lib/gemini/embedText.ts` (single-text sibling to `lib/ingestion/embed.ts`'s batch embedder, left untouched to avoid any risk to working Phase 3 code) and a "Matching jobs" panel on the resume editor page (`components/editor/MatchingJobsPanel.tsx`), following the same collapsible-panel pattern as "Tailor for a job." Chose a per-resume matching view over surfacing scores on `/dashboard/jobs` — the job search page has no natural "which resume" context, while every resume already owns its own page. Wrote `e2e/match-scoring.spec.ts` (reuses the same real Ashby/Linear postings `job-ingestion.spec.ts` seeds); first run hit a Playwright strict-mode locator collision (`getByText("Matching jobs")` also matched the "Find matching jobs" button text) — fixed with `exact: true`. Hit one infrastructure snag unrelated to the code: `npm run build` hung indefinitely with near-zero CPU usage, the same OneDrive/`.next`-trace file-lock symptom recorded in Phase 2's session log; confirmed via `Get-Process` that the build's own node processes were genuinely stalled (not just slow), asked the user before force-killing them since that's exactly the kind of action that shouldn't happen silently, then cleared `.next` and reran clean. Final verified state: `lint`/`tsc`/`test` (23)/`build`/`test:e2e` (5/5, including the new test) all pass.
- **2026-07-26/27** — User merged Phase 4 and, before starting Phase 5, raised two concerns: (1) job postings are never re-checked for staleness, so the DB only grows and filled/closed roles stay searchable forever; (2) wanted a proper job application tracker with per-application detail and, specifically, automatic removal-from-board + notification when a saved job's posting closes. Most of (2) was already scoped as Phase 7 ("Application tracking"); asked the user two questions rather than guessing — where staleness detection should land in the roadmap, and what notification channel to use for the removal alert. Landed on: fix staleness now as a small pre-Phase-5 patch (not bundled into Phase 7, since the DB is growing unbounded today, not just once a tracker exists), and in-app-only notifications (no email, so the Phase 8 custom-SMTP item doesn't need to move up). Built the fix on `fix/job-posting-staleness` (branched from `main`, not `phase-5`, to keep it a separate focused PR): `job_postings.status` (`active`/`closed`, migration `0004_job_posting_status.sql`) as a soft-delete — `applications.job_posting_id` references these rows, so a hard delete would blank out a user's application history the moment a job closed; `lib/ingestion/run.ts` now closes any posting missing from a company's current fetch and re-activates any that reappear, gated on the fetch itself having succeeded so a transient network blip can't wrongly close everything; both matching RPCs and the job search page filter to `status = 'active'`. Added the removal/notification UI as a new Phase 7 backlog item rather than building it now, since it depends on the tracker existing first. Hit the build-hang issue *again* during testing, this time with a different signature: `npm run build` ran ~30 minutes with CPU climbing steadily the whole time (not near-zero like the first occurrence) yet zero files written to `.next/` after the first few seconds — confirmed via repeated `Get-Process` CPU checks and directory-timestamp checks that it was genuinely stuck despite the CPU usage, not just slow; asked the user again before killing since the symptom differed from the documented case, then cleared `.next` and reran clean in seconds — same underlying fix, new data point on how this hang can present. Final verified state: `lint`/`tsc`/`test` (25, including new `lib/ingestion/run.test.ts`)/`build`/`test:e2e` (6/6, including two new/extended tests in `job-ingestion.spec.ts`) all pass. Ready for its own PR to `main` before Phase 5's career-profile work begins.
- **2026-07-27** — User confirmed the staleness fix was merged (PR #9) and to continue to Phase 5, and separately flagged (not urgent — "at some point") that the front-end needs a real visual polish pass, functionality-first Tailwind styling throughout so far. Ran the phase handoff (pulled `main`, deleted the merged fix branch, fast-forwarded `phase-5` onto the new `main`) and logged the front-end note under Phase 8 rather than scoping it now, since the user framed it as future work, not immediate. Built Phase 5: three new tables (`projects`, `education`, `work_experience`, migration `0005_career_profile.sql`), a GitHub README fetcher (`lib/github/fetchRepo.ts`, distinct error messages for a 404/403/network failure rather than one generic one) feeding a one-time Gemini summarization (`lib/gemini/summarizeProject.ts`) at project-creation time, a `/dashboard/profile` UI with three sections using inline `<details>`-based edit forms rather than dedicated per-item pages (these are plain structured fields, not rich text, so a full editor page like resumes/cover-letters use would've been overbuilt), and wired the profile data into `tailorResume.ts`'s prompt so it's not collected-but-unused. Landed on a parallel `PROJECT_LIMITS` tier-limit helper rather than forcing projects into `lib/documents/db.ts`'s `BASE_LIMITS`, since projects don't have resumes/cover-letters' base/tailored-document split. The e2e test (`e2e/career-profile.spec.ts`) went through several real failures before passing, each a genuine bug in the test itself, not the feature: (1) a transient `fetch failed` against GitHub's API on the first run — confirmed transient by rerunning, consistent with the same intermittent `github.com` DNS blips already seen with `git push` this session; (2) a Playwright strict-mode violation where a plain `getByText` locator ambiguously matched multiple elements inside a `<details>`/`<summary>` structure (the project title *and* its generated description) — fixed by scoping to `summary` specifically; (3) the same issue on the edited-description assertion, matching both the display `<p>` and the revalidated `<textarea>`'s value — fixed by scoping to `p`; (4) the real one worth noting for next time: the tailoring assertion's `waitForURL` predicate matched the *current* (pre-navigation) resume URL immediately since it didn't exclude the base resume's own pathname — silently passing without ever waiting for the actual navigation, so the test read the *original*, untailored text and only failed on the final content assertion. `ai-features.spec.ts` already handles this correctly (excludes the base URL explicitly); this test just hadn't copied that detail. Once fixed, the test reliably confirms the career-profile data changes tailoring output: a deliberately fictional, distinctively-named project ("Quantum Widget Scheduler," Rust/gRPC) never mentioned in the test resume shows up in the tailored result when the job description matches it closely. Final verified state: `lint`/`tsc`/`test` (35)/`build`/`test:e2e` (7/7) all pass.
- **2026-07-27** — User confirmed Phase 5 was merged (PR #10) and to continue to Phase 6 (Billing). Before writing any code, asked whether real Stripe credentials existed yet, since payments needs real inputs a placeholder can't cover — user asked whether a free alternative to a real Stripe account existed; clarified that Stripe's test mode already *is* free (no card required, fake test cards simulate real charges/subscriptions/webhooks) and isn't a separate thing from the real integration, just run against fake data — so the choice was really about scope/timing, not cost. Asked a follow-up: build the real Stripe integration now (free test mode), or stub tier-gating behind a manual toggle and defer real checkout/webhooks to a later pass. User chose to set up Stripe test mode now, to avoid throwaway work later. User created a Stripe account, a Pro-tier product, and pasted the publishable + secret keys directly into chat along with a **Product ID** (`prod_...`) rather than a **Price ID** (`price_...`) — checkout needs the latter specifically, since a product can have multiple prices. Looked up the actual price via the Stripe API using the secret key rather than asking the user to go find it manually; found one already existed at exactly $9.99/month, used as the Pro-tier placeholder. Saved all three values to `.env.local` without echoing them back in chat, per standing instruction.
  Fixed `lib/stripe/client.ts` to the lazy-singleton pattern before building anything on top of it — it had been left as Phase-0-era top-level `new Stripe(...)` construction, the exact anti-pattern already fixed for Gemini in Phase 2. Installed the Stripe CLI via `winget install --id Stripe.StripeCli` (needed the exact package ID — `Stripe.StripeCLI` with a capital CLI doesn't match; `winget search stripe` found the real one). Built webhook handling (`app/api/webhooks/stripe/route.ts`) and checkout/portal Server Actions (`lib/stripe/actions.ts`). Before writing the Playwright e2e test, manually validated the whole subscription lifecycle against the real (test-mode) webhook endpoint using `stripe listen` + `stripe trigger` directly from the shell — faster to iterate on than through Playwright, and it caught two real things: (1) `stripe trigger checkout.session.completed`'s default fixture is a **one-time payment**, not a subscription, so the handler correctly no-op'd on `session.mode !== "subscription"` — switched to triggering `customer.subscription.created/updated/deleted` directly instead, using `--override subscription:metadata[user_id]=<id>` to link the synthetic event to a real throwaway user; (2) a genuine bug: `profiles.stripe_customer_id` never got backfilled for a subscription that arrived via a webhook event not tied to our own checkout flow (exactly what `stripe trigger` produces, but also a real scenario -- e.g. a subscription touched directly in the Stripe dashboard) — `createPortalSession` would have silently failed to find a customer id to open a portal for. Fixed by having `upsertSubscription` set `stripe_customer_id` unconditionally on every sync, not just at checkout-creation time; re-verified the fix live before writing the automated test. Also confirmed via the installed Stripe SDK's own type definitions (not assumed) that `current_period_end` lives on each subscription *item*, not the top-level `Subscription` object, in this API version — extracted into a small, unit-tested `webhookHelpers.ts` rather than leaving it inline and untested.
  Also built the carried-forward Phase 2 item: per-user AI generation rate limiting (`ai_generation_events` table, migration `0006_ai_rate_limit.sql`, rolling 24h window; placeholder limits of 10/day free, 100/day pro), wired into all three real Gemini `generate_content` call sites. Hit the same GitHub/Supabase/Stripe transient DNS blips already seen repeatedly this session (`git push`, GitHub API, Supabase auth API) two more times against `api.stripe.com` and the Supabase auth API during manual testing and the e2e run — confirmed transient by retrying each time, not a code issue. Final verified state: `lint`/`tsc`/`test` (41, including new tests for `tierForSubscriptionStatus` and the webhook helpers)/`build`/`test:e2e` (8/8, including the new Stripe-CLI-driven `e2e/billing.spec.ts`) all pass.
- **2026-07-27** — User confirmed Phase 6 was merged (PR #11), asked to proceed to Phase 7, and separately asked for a dedicated phase for front-end beautification/polishing (previously just a single flagged-not-scoped bullet under Hardening, added earlier this session at the user's "at some point" request). Restructured the roadmap: promoted it to its own **Phase 8**, inserted right after Application tracking and before Pre-launch hardening (all core functional surfaces exist by then, so there's a full app to actually look at and polish; nothing about it technically depends on or blocks Hardening/Launch) — renumbered old Phase 8 (Hardening)→9 and Phase 9 (Launch)→10, and fixed the several stale in-backlog cross-references that renumbering broke (the cron-not-live-until note, the Vercel-plan-decision note, the custom-SMTP note). Didn't ask the user where exactly to insert it, unlike the earlier career-profile/Phase-4-vs-5 sequencing question — that one had a real functional dependency to weigh; this one doesn't, so a reasonable default seemed better than another round-trip. Ran the phase-start checklist (pulled `main`, deleted the merged `phase-6` branch, created and pushed `phase-7`) before starting the actual Phase 7 work.
  Built Phase 7. `applications` already had a full schema since `0001_init.sql` (job/resume/cover-letter links, a `status` check constraint, `notes`) — no new migration needed for the tracking pipeline itself, only for notifications. Added `listAllDocuments` to `lib/documents/db.ts` (every resume/cover letter a user has, not just base ones via `listBaseDocuments`) specifically so an application's "resume used" picker can point at a tailored copy, not just a base document — otherwise the whole point of linking "the specific version used" would be lost. Built `/dashboard/applications` with an "Active" and a "Closed postings" section, reusing the inline `<details>`-based edit-form pattern from Phase 5's profile sections rather than inventing a new one. For the removal-from-board + notification requirement (scoped back on 2026-07-26, built now): extended `lib/ingestion/run.ts`'s existing closure-detection block (already tracking which postings go missing from a fetch) to also look up affected applications and insert a `notifications` row (new table, migration `0007_notifications.sql`, in-app only as already decided) for each affected user — kept as a best-effort side step (errors logged, not thrown) so a notification glitch can't fail an otherwise-successful ingestion run. Added a `NotificationBell` in the dashboard header (unread count + dismiss).
  `e2e/applications.spec.ts` reuses the same technique as the earlier job-posting-staleness closure test: insert a fake, controlled posting rather than depending on real ATS data being able to "disappear" on demand, save it via the actual UI, update its status and link a resume, then run real ingestion against Linear (Ashby) to trigger its closure and verify the full chain — board update, "Closed postings" section, and a real notification row with the right message, dismissible via the bell. Passed on the first real run, no bugs found this time. This is now the third e2e file reusing the small Ashby/Linear ingestion as a shared test vehicle (after `job-ingestion.spec.ts` and `match-scoring.spec.ts`) — noting the accumulating per-run cost/time this adds to the full suite, not a problem yet but worth watching if more tests lean on it. Final verified state: `lint`/`tsc`/`test` (41)/`build`/`test:e2e` (9/9) all pass.
- **2026-07-28** — User confirmed Phase 7 was merged (PR #12) and asked to proceed to Phase 8 (front-end visual polish). Ran the phase handoff (pulled `main`, deleted the merged `phase-7` branch, created and pushed `phase-8`). Since Phase 8's own backlog explicitly flagged real open design-direction questions rather than guessing at them, asked two: component library vs. refined custom Tailwind, and stay-minimal vs. a more distinct visual identity. User chose shadcn/ui and a distinct identity. For the actual color, rather than trying to specify an exact color in words back and forth, picked a concrete direction (indigo, common for professional/career SaaS) and just built it — colors are something to react to visually, not negotiate over text.
  Initialized shadcn/ui (`npx shadcn@latest init`) — this install resolves to Base UI primitives (not Radix) under the "base" style, worth knowing since Base UI's prop/behavior surface differs slightly from Radix-based shadcn docs found online. Defined the indigo brand accent as theme tokens in `app/globals.css` (light + dark), leaving borders/backgrounds/muted neutral gray — one deliberate accent color, not a full custom palette. Added the core primitives (`Button`, `Input`, `Textarea`, `Select`, `Card`, `Badge`, `Collapsible`, `DropdownMenu`, `Separator`, `Checkbox`, `Alert`, `Skeleton`) then made a deliberate scoping call before touching any page: verified by reading their source that `Input`/`Textarea`/`Card`/`Badge`/`Label`/`Separator`/`Alert`/`Button` are thin wrappers around real native elements (safe, zero risk to the app's `FormData`-based Server Action forms), while `Select`/`Checkbox`/`Collapsible`/`DropdownMenu`/`Dialog` are fully custom Base UI widgets with their own form-participation and interaction models. Given the entire e2e suite depends on plain `<form action={serverAction}>` submission and `<details>/<summary>` expand-collapse working exactly as today, kept native `<select>`/`<input type="checkbox">`/`<details>` (styled to visually match the new system) rather than swapping to the custom equivalents — achieves the same polished look without re-verifying form/FormData behavior for every single interactive control in the app. Tried shadcn's `DropdownMenu` specifically for `NotificationBell` (a natural-seeming fit) and reverted it too, once its Menu-item primitive didn't cleanly host a nested per-notification `<form>` with its own submit button.
  Rebuilt every page and shared component: landing, login/signup/check-email, the dashboard shell (nav + `NotificationBell`), dashboard overview, resumes/cover-letters (list pages, `DocumentEditor`, `EditorToolbar`, `TailorResumeForm`, `MatchingJobsPanel`, `GenerateCoverLetterForm`, `TailoredVersionsList`), jobs search, the applications board (`ApplicationRow`), all three profile sections, and billing. Added `loading.tsx`/`error.tsx` at both the root and `app/dashboard/` (Skeleton-based loading, Alert-based error boundary with a retry button) — closes the Phase 2 carried-forward gap. Preserved every existing e2e-facing detail deliberately: `data-testid`s, exact button/placeholder text, and label-to-input association (switched from nested `<label>Text<input/></label>` to explicit `htmlFor`/`id` pairs using shadcn's `Label`, which resolves to the same accessible name Playwright's `getByLabel` needs).
  First full e2e run surfaced a real, non-obvious markup bug across *every single* collapsible panel and list-row component that got touched: `<details><Card><summary>...` — wrapping `<summary>` inside the `Card` div means it's no longer a *direct* child of `<details>`, which is a hard HTML requirement for the browser to treat it as the always-visible disclosure control. Without a valid direct-child `<summary>`, the browser falls back to hiding the entire contents (summary included) until an internal default toggle is used — so every "Tailor for a job," "Matching jobs," "Generate with AI," each project/education/work-experience/application row, all failed with "element is not visible" or "not found," not a styling issue but a structural one. Fixed uniformly by swapping the wrapper order to `<Card><details><summary>...` in all seven affected components. Two further failures on the full-suite run (`billing.spec.ts`'s `stripe listen` readiness timeout, `career-profile.spec.ts`'s GitHub-fetch-adjacent flake) both passed cleanly in isolation on retry — consistent with the transient network blips already logged repeatedly this session, not caused by the rewrite. Also updated `e2e/applications.spec.ts`'s notification-bell assertions to match the new `Badge`-based unread count (was plain "Notifications (1)" text, now "Notifications" + a separate Badge element). Final verified state: `lint`/`tsc`/`test` (41)/`build`/`test:e2e` (9/9) all pass.
- **2026-07-28** — Before merging Phase 8, user gave direct negative feedback on the actual visual result: the indigo/neutral look "looks as if someone just began to code," wanted something more welcoming, and pointed at builtin.com as a reference. Also asked for an actual dark mode toggle — turned out there wasn't one to begin with; the `.dark` class the whole theme system depends on was never being applied by anything, so dark mode was silently unreachable regardless of OS setting. Continued on the same `phase-8` branch (PR not yet merged) rather than opening a new phase for a revision of unmerged work.
  Built a real toggle: a synchronous inline script in the root layout applies the stored (`localStorage`) or OS-default theme class before first paint (no flash), and `components/theme/ThemeToggle.tsx` flips it client-side and persists the choice. Rather than iterate on the exact accent color through several rounds of text description (imprecise for something this visual), picked a concrete warm coral/orange-red direction — Built In's own brand leans this way, and it reads as more human/approachable than the previous cool indigo — and just built it, same "propose a direction, let them react to the real thing" approach used for the original brand-color decision. Revised `app/globals.css`'s theme tokens: warm coral primary (oklch hue ~38, deliberately kept apart from `--destructive`'s red at hue ~27 so a delete button and a primary CTA don't read as the same color), warm off-white/charcoal neutrals instead of stark white/black, `--radius` bumped from `0.625rem` to `1rem`, and `Card` given a soft shadow instead of a bare thin ring — all changes that cascade automatically to every page already built on these tokens, no per-page rework needed. Added a shared `SiteHeader` (logo, sign in/get started, theme toggle) to the landing page and login/signup/check-email for a consistent marketing shell, and gave the landing page colorful per-feature icon chips pulling from the new `--chart-*` tokens.
  Did not just trust the build passing this time — took a screenshot of the landing and login pages in both light and dark mode via a throwaway Playwright script (`node` directly, not through the e2e suite) and actually looked at the rendered output before calling this done, per the standing instruction to verify UI changes in a browser rather than assume. Confirmed the toggle genuinely switches themes and the warm palette renders as intended in both modes. Re-ran the full testing gate after the token/component changes: `lint`/`tsc`/`test` (41)/`build`/`test:e2e` (9/9) all pass, no regressions from the revision.
- **2026-07-30** — User reported "a build error" with no details. Tried to reproduce with a clean `.next` + `npm run build` — passed cleanly here, so asked where they were seeing it (Vercel vs. local) rather than guess at a fix for an error nobody had actually seen the text of; user confirmed local `npm run build`/`npm run dev`, suggested checking for a stale `node_modules` after the recent dependency-heavy commits, but the conversation moved on before that was confirmed either way.
  User's very next message, without addressing the build error directly, said the font "looks... plain... like an email or essay" and that text felt too small for good legibility. Recognized these as probably the same root cause: `next/font/google`'s Geist fetch had already been observed failing in this exact environment (a dev-server warning logged earlier this session, "Failed to download `Geist`... using fallback font"), and a silent fallback to the browser's default serif is exactly what "looks like an essay" describes — a plausible, well-evidenced connection to the build report too, even without the literal error text. Fixed the network dependency at the root by **self-hosting** a replacement typeface (Plus Jakarta Sans, downloaded as a variable woff2 to `app/fonts/`, loaded via `next/font/local`) instead of trying to make the Google Fonts fetch more reliable, and dropped `Geist_Mono` for a plain system monospace stack (only used incidentally in Tiptap code blocks).
  Verifying this visually (screenshotting again, not just trusting the build) surfaced a second, more fundamental, independent bug: the font loader's CSS variable was being applied via `className` on `<body>`, while `globals.css` applies the `font-sans` utility on `<html>` — and CSS custom properties cascade only downward, from ancestor to descendant, never from a child back up to its parent. `<html>` could never see a variable defined on its own child, so **no font had ever actually applied**, for Geist or the new self-hosted one — a bug independent of and prior to the network-fetch issue, that would have kept silently falling back to the browser default even with a perfectly reliable font source. Fixed by moving the font `className` onto `<html>` instead of `<body>`; re-screenshotted and could clearly see real Plus Jakarta Sans letterforms this time, not a serif fallback, in both light and dark mode.
  Separately fixed the "everything seems a little small" legibility complaint: bumped root `html` font-size from the 16px default to 18px (scales every rem-based Tailwind size/spacing utility site-wide in one change), removed shadcn's `md:text-sm` desktop-downsize on `Input`/`Textarea` (was working directly against the goal), and bumped `Button`/`Input`/`Textarea`/`Label` default heights/text a step larger. Re-ran the full testing gate: `lint`/`tsc`/`test` (41)/`build`/`test:e2e` (9/9, one transient Supabase auth `fetch failed` on the full run — passed cleanly alone on retry, consistent with the same class of network blip logged repeatedly this session) all pass.
